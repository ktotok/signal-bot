# Secrets and account credentials — must never be committed.
.env
signal-cli-config/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Local Claude Code settings (per-machine, do not commit)
.claude/settings.local.json
